[package]
name = "pixelizer"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
image = "*"

[profile.release]
strip = "symbols"
lto = "fat"
codegen-units = 1
opt-level = 3
debug = false
rpath = true